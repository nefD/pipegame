export const S_PIPE = {
  i: 'pipe-I',
  l: 'pipe-L',
  t: 'pipe-T',
  cross: 'pipe-cross',
  single: 'pipe-single',
};

export const TILE_WIDTH = 32;
export const TILE_HEIGHT = 32;

export enum Direction {
  north,
  east,
  south,
  west,
}
